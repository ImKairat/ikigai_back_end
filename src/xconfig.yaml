
server:
    host: localhost
    port: 8080

database:
    postgres:
        host: localhost
        port: 5432
        user: postgres
        password: postgres
        database: postgres
        table: postgres

    mongo:
        host: localhost
        port: 27017
        user: mongo
        password: mongo
